node_modules/
dist/
*.local
.DS_Store
.vite/

# NOTE: public/assets/ holds the processed art and IS committed, because the
# high-res source in /Assets is no longer present to regenerate it (and the
# Vercel build does not run `npm run preprocess`). Do not ignore it.

# Logs & test artifacts
*.log
scripts/shots/
